import React from 'react';
import { Typography } from '@mui/material';

const Users = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>Users</Typography>
      <Typography>Manage users and permissions.</Typography>
    </div>
  );
};

export default Users;