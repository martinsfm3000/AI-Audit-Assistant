import React from 'react';
import { Typography } from '@mui/material';

const Settings = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>Settings</Typography>
      <Typography>Configure application settings.</Typography>
    </div>
  );
};

export default Settings;