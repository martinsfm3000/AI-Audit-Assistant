import React from 'react';
import { Typography } from '@mui/material';

const Reports = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>Reports</Typography>
      <Typography>Generate and view audit reports.</Typography>
    </div>
  );
};

export default Reports;